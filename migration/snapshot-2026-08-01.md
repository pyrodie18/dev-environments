# Pre-migration snapshot — 2026-08-01

This snapshot intentionally excludes credential values, environment-file contents, Codex tokens, session databases, and repository diffs outside the legacy shared devcontainer.

## Host

- Rocky Linux 9.7
- Docker 29.5.2
- VS Code Server 1.131.0
- Codex CLI supplied by the VS Code extension: 0.146.0-alpha.9.2

## Legacy layout

- `~/devel/ansible` is the one-commit `pyrodie18/ansible-devcontainer` repository.
- `~/devel/python/.git` and `~/devel/.git` are empty directories, not valid Git repositories.
- Shared Codex state exists under `~/devel/.codex-state/{python,ansible}`.
- Both language devcontainers referenced one shared ChatGPT authentication file.
- Both legacy `devcontainer.env` files defined an `OPENAI_API_KEY` variable.

## Clean pilots

- `~/devel/python/gmail_mcp`: clean `main`, no origin configured.
- `~/devel/ansible/collections/ansible_collections/pyrodie18/utils`: clean `main`, origin `pyrodie18/ansible_utils`.

## Repositories intentionally preserved in place

The following had uncommitted or untracked work and are excluded from automatic relocation:

- `community/zabbix`
- `pyrodie18/splunk`
- `sausage_sensor/ss`
- `python/AdventOfCode`
- `python/log_generator`
- `python/splunk-conf-docs`
- `python/zeek_generator`
- `ss/ss`

## Legacy shared Ansible devcontainer diff

The tracked working-tree diff is stored in `legacy/ansible-devcontainer-working-tree.patch`. The untracked environment file is deliberately excluded because it contains a credential variable.

The complete outer repository history is also stored in
`legacy/ansible-devcontainer-history.bundle`. `git bundle verify` confirms that
it contains `main`, `origin/main`, and `HEAD` and records complete history.

## Pilot relocation

The clean `pyrodie18/utils` pilot moved to
`~/devel/ansible/pyrodie18.utils`. Its Git metadata and configured origin moved
with it. The original nested path is now absent; no other collection directory
was changed.

After confirming that the two organizer `.git` paths contained no objects,
configuration, or refs, they were removed from the host. No repository history
was removed by that cleanup.
