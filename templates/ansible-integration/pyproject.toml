[project]
name = "__PROJECT_SLUG__-integration"
version = "0.0.0"
requires-python = ">=3.12,<3.13"
dependencies = []

[dependency-groups]
dev = [
    "ansible-core>=2.20.0,<2.21",
    "pytest>=8.3.2",
]

[tool.uv]
package = false
